package classesMain.LeetCode;

public enum EXTRA {

    /*
    private static List<Integer> getDivisors(Integer key) {
        List<Integer> myList = new ArrayList<>();
        int factors = 0;
        for (int i = 1; i <= Math.sqrt(key); i++) {
            if (i % key == 0) {
                if (i < Math.sqrt(key)) {
                    factors += 2;
                    if (i != key / i) { // Check if n/i is a different divisor
//                        System.out.print("key / i ");
//                        System.out.print(key / i + " ");
                    }
                } else {
                    factors++;
                }
            }
        }
//        System.out.println(factors);
        myList.add(factors);
        return myList;
    }
*/
}
